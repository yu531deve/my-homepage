<section id="hero" class="hit-layer relative min-h-screen overflow-hidden px-6 md:h-screen">
  <div class="scene-inner absolute inset-0 flex flex-col items-center justify-center gap-6">
    <div
      class="hero-stage relative flex flex-col items-center justify-center"
      style="perspective: 1000px; transform-style: preserve-3d;"
    >
      <p
        class="hero-layer--back gl-text text-fg pointer-events-none absolute inset-0 flex items-center justify-center text-[28vw] leading-none font-bold whitespace-nowrap opacity-[0.06]"
        style="-webkit-text-stroke: 1px currentColor; color: transparent;"
        data-gl-anchor="hero-back"
        aria-hidden="true"
      >
        PORTFOLIO 2026
      </p>
      <h1
        class="hero-layer--main text-fg text-center text-[clamp(4rem,17vw,15rem)] leading-[0.85] font-bold tracking-[-0.04em]"
      >
        <span class="hero-heading gl-text block" data-gl-anchor="hero-yudai">YUDAI</span>
        <span class="hero-heading gl-text block" data-gl-anchor="hero-harada">HARADA</span>
      </h1>
    </div>
    <div class="hero-layer--front flex flex-col items-center gap-6">
      <p class="hero-sub gl-text text-fg-muted text-lg" data-jp data-gl-anchor="hero-sub">
        エンジニア / つくることが好きです。
      </p>
    </div>
  </div>
  <div
    class="hero-scroll-hint gl-text text-fg-subtle absolute bottom-10 left-1/2 -translate-x-1/2 text-xs tracking-[0.3em]"
    data-gl-anchor="hero-scroll"
  >
    SCROLL
  </div>
</section>

<style>
  @keyframes hero-scroll-hint {
    0%,
    100% {
      transform: translateY(0);
    }
    50% {
      transform: translateY(6px);
    }
  }

  html:not(.webgl-on) .hero-scroll-hint {
    animation: hero-scroll-hint 1.8s ease-in-out infinite;
  }
</style>
